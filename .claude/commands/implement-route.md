# Implement a portfolio route

Use `$portfolio-context` and `$implement-portfolio-route` to implement
`$ARGUMENTS` in the real repository.

Inspect before creating, preserve both locales and confidentiality, reuse the
design system, implement real states, add tests, validate representative widths
and keyboard behavior, and run the proportional package scripts. Return the
implemented result rather than only a plan or audit.
