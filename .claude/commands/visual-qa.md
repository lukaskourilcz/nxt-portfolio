# Portfolio visual QA

Invoke the `ux-visual-qa` agent for `$ARGUMENTS`. Inspect rendered routes with
the repository’s browser tests at 320, 390–430, 768, 1024–1280, 1440, and wide
desktop widths. Check both locales, all flagship case studies, system pages,
real non-happy states, reduced motion, and console errors. Make file-specific
fixes when implementation is requested and rerun the affected checks.
