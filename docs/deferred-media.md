# Deferred media slots

Higgsfield AI is unavailable in the current implementation environment. No
substitute generator, placeholder asset, prompt research, or provisional raster
was used. The current interface is complete with authentic imagery and
code-native diagrams; these optional slots can accept art-directed assets later
without restructuring the routes.

| Slot | Integration point | Intended purpose | Delivery contract |
| --- | --- | --- | --- |
| Hero system-boundary visual | `HeroSection.tsx`, adjacent to the authentic portrait | Add a restrained identity layer about boundaries, responsibility, and signal flow without replacing or altering the portrait | Decorative or succinct localized alt; AVIF/WebP desktop/mobile crops; no loop required; strict above-fold budget |
| Banking modernization cover | `BankingVisual` in `src/components/project-visual.tsx` | Abstract controlled migration, BFF mapping, contracts, tests, and secure boundaries; explicitly not a real banking UI | Desktop/mobile stills, abstract caption retained, no interface text/logos/confidential detail |
| Ersilia workflow cover | `ScienceVisual` in `src/components/project-visual.tsx` | Editorial paper-to-schema-to-validation workflow without implying scientific results | Desktop/mobile stills, no fabricated chart/findings, caption retained |
| devShark presentation environment | `AuthenticProductVisual` in `src/components/project-visual.tsx` | Add controlled depth/framing around the exact existing screenshot | Deterministic composite only; screenshot pixels/text remain exact; responsive crops |
| Homepage social image | `src/app/(site)/[locale]/opengraph-image.tsx` | Optional art-directed social system beyond the current code-rendered branded fallback | 1200×630, text-safe, localized output or text-free composition, no generated lettering |
| Case-study social images | localized work `opengraph-image.tsx` | Distinct banking/Ersilia/devShark unfurls while preserving the shared identity | 1200×630 each locale/project; no confidential/fake UI; readable at preview size |
| Optional controlled motion | Project visual components only where motion explains migration/validation | Show path resolution, validation, or layer alignment | Short WebM/MP4, no audio, poster still, lazy delivery, pause/static fallback, reduced-motion exclusion |

Record provenance, selected/rejected variants, responsive exports, optimization,
and regeneration instructions only when the actual integration is available and
generation occurs. Remove the corresponding code-native fallback only after the
new asset passes responsive, accessibility, authenticity, and performance QA.
